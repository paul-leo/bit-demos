---
description: 'A app-shell app.'
labels: ['app', 'react']
---

A React application for app-shell. 

## Run the application

Import the app and add the following config to your workspace.jsonc:

```
bit use app-shell 
```

To run the app locally, run the following:

```
bit run app-shell 
```
